const fs=require('fs'),path=require('path');const base=path.resolve(__dirname,'..'),results=path.join(base,'test-results');
const before=require('../validation/DEPLOYMENT-INVENTORY-0.2.0.json'),after=require('../dist/DEPLOYMENT-INVENTORY.json'),old=new Map(before.files.map(f=>[f.path,f.sha256]));const changed=after.files.filter(f=>old.get(f.path)!==f.sha256).map(f=>f.path);
fs.writeFileSync(path.join(base,'validation/MEDIA-CHANGED-FILES.txt'),changed.join('\n')+'\n');
for(const file of ['report','links','pixels','live','layout'])fs.copyFileSync(path.join(results,'media-after',file+'.json'),path.join(base,'validation/media-'+file+'.json'));
let html='<!doctype html><html lang="fr"><meta charset="utf-8"><title>AI Weather — avant/après</title><style>body{font:16px system-ui;background:#edf2f7;color:#172334;margin:24px}section{margin:36px 0}figure{margin:0}img{width:100%;height:auto} .pair{display:grid;grid-template-columns:1fr 1fr;gap:20px;align-items:start}a{color:#125ca6}figcaption{font-weight:bold;padding:12px;background:white}</style><h1>Retrait de For Media — comparaison avant/après</h1><p>Captures du staging, 16 septembre 2026. Modifications prévues : footer Company, bloc final How It Works et renvoi Contact. Le globe USA est animé. Les captures isolées du nouveau bloc masquent le header fixe uniquement pendant la prise de vue.</p>';
for(const slug of ['home','how-it-works','widgets','today','stations','daily-brief','about','contact','us-station','quiz'])for(const width of [1440,390]){html+='<section><h2>'+slug+' — '+width+' px</h2><div class="pair">';for(const [folder,label] of [['media-before','Avant'],['media-after','Après']]){const src=folder+'/'+slug+'-'+width+'.png';html+='<figure><figcaption>'+label+'</figcaption><a href="'+src+'"><img loading="lazy" src="'+src+'"></a></figure>';}html+='</div></section>';}
html+='<h2>Nouveau bloc, français et anglais</h2>';for(const width of [1440,390])for(const lang of ['fr','en'])html+='<p><a href="media-after/new-block-'+width+'-'+lang+'.png">'+lang.toUpperCase()+' — '+width+' px</a></p>';
html+='<p><a href="media-before/for-media-1440.png">For Media : contenu public avant retrait</a></p></html>';fs.writeFileSync(path.join(results,'media-comparisons.html'),html);console.log('Report: '+changed.length+' theme files; 20 screenshot pairs.');
