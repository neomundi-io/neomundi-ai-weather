const fs=require('fs'),path=require('path'),assert=require('assert/strict');let pw;for(const f of fs.readdirSync(path.join(process.env.LOCALAPPDATA,'ms-playwright/.links'))){try{pw=require(fs.readFileSync(path.join(process.env.LOCALAPPDATA,'ms-playwright/.links',f),'utf8').trim());break;}catch{}}
(async()=>{const b=await pw.chromium.launch({headless:true});const c=await b.newContext();await c.addCookies([{name:'playground_auto_login_already_happened',value:'1',url:'http://127.0.0.1:9400'}]);await c.route('**/*',r=>['127.0.0.1','localhost'].includes(new URL(r.request().url()).hostname)?r.continue():r.abort());const p=await c.newPage();await p.goto('http://127.0.0.1:9400/today/',{waitUntil:'networkidle'});const results=[];for(const lang of require('../source-public/config/languages.json').available){await p.locator('#lang-button').click();await p.locator('.lang-option[data-lang="'+lang.code+'"]').click();await p.waitForTimeout(300);const state=await p.evaluate(()=>({cards:document.querySelectorAll('#wall-native [role=listitem]').length,historyVisible:!document.querySelector('#history-content').hidden,direction:document.querySelector('#history-content').getAttribute('dir')}));assert.equal(state.cards,12);assert(state.historyVisible);if(lang.code==='ar')assert.equal(state.direction,'rtl');results.push({lang:lang.code,...state});}fs.writeFileSync('test-results/languages.json',JSON.stringify(results,null,2));console.log('PASS: 13 languages; 12 cards and history visible; Arabic RTL.');await b.close();})().catch(e=>{console.error(e);process.exitCode=1;});
